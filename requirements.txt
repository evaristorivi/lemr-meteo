# Core frameworks: permite parches de seguridad pero bloquea breaking changes
Flask>=3.0.2,<4.0
Flask-Limiter>=3.5.0,<5.0
gunicorn>=21.2.0,<22.0

# Utilidades: versión compatible
python-dotenv>=1.0.0,<2.0

# HTTP: versión mínima segura
requests>=2.32.0,<3.0

# APIs externas: flexible (cambian frecuentemente)
openai>=2.21.0

# Conteo exacto de tokens para estimación del payload
tiktoken>=0.7.0
